package bomb.enumerations;

public enum CFlash {
    ;
    public enum Word{
        RED, YELLOW, GREEN, BLUE, MAGENTA, WHITE
    }

    public enum Color{
        RED, YELLOW, GREEN, BLUE, MAGENTA, WHITE
    }
}
