package bomb.abstractions;

/**
 * This interface deals with any enum that needs to list out an array of Strings
 */
public interface Listed {
    /**
     * Returns the array of Strings
     *
     * @return The array of Strings
     */
    String[] getWords();
}
